# 用法：复制成 .env 再填值
#   cp .env.example .env          # macOS / Linux / Git Bash
#   copy .env.example .env        # Windows cmd
#
# .env 已被 .gitignore 忽略，key 不会进版本库。格式：一行一个 KEY=VALUE。

# ---- 必填 ----
# DeepSeek API key，申请地址 https://platform.deepseek.com/api_keys
# 变量名不能改：app/llm/client.py 读的就是 DEEPSEEK_API_KEY。
DEEPSEEK_API_KEY=sk-在此填入你的key

# ---- 可选：网络代理 ----
# 若本机必须走代理才能连上 api.deepseek.com，就取消下面两行注释。
#
# 为什么要写在文件里而不是靠系统代理：httpx **只读环境变量，不读 Windows
# 注册表里的系统代理设置**（ProxyEnable=1 也不管用），所以"系统代理开着"
# 和"Python 能连上"是两件事。实测症状是 APIConnectionError，异常链最底层
# 是 ssl.SSLEOFError: EOF occurred in violation of protocol；而同机
# `curl https://api.deepseek.com` 返回 401 —— curl 通不等于 Python 通。
#
# client.py 加载本文件时用的是 os.environ.setdefault，所以这里填了就全局
# 生效，不用每次在命令行前缀一遍。端口按你自己代理软件的实际配置改。
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
